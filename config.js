// Configuración de API
const API_URL = 'https://TU_API_GATEWAY_URL/prod/api';

// Exportar para uso en otros archivos
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { API_URL };
}
